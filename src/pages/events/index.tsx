import { Box, Container, Typography } from '@mui/material';

export default function DiscoverEvents() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Discover Events
      </Typography>
      {/* We'll add the event listing and filters here */}
    </Box>
  );
} 