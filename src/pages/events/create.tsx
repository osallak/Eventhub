import { Box, Container, Typography } from '@mui/material';

export default function CreateEvent() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Create New Event
      </Typography>
      {/* We'll add the event creation form here */}
    </Box>
  );
} 